URL="https://sys.adminpy.com:18001/api/incidents/"
TOKEN="0e662de2f71e54ef88002e7deb4b52666d961621"     # tu token
FILE="./data/incidents_560.json"

jq -c '.[]' "$FILE" | while read -r row; do
  curl -s -X POST "$URL" \
       -H "Authorization: Token $TOKEN" \
       -H "Content-Type: application/json" \
       -d "$row"
done